import StreetButton from '../component/StreetButton';

function FormView() {
    return (
        <div className="FormView">
            {/* {filteredStreets.map((s: any) =>
                <StreetButton name={s.properties.name as string} isConfigured={false} lastUpdateDate='toto'></StreetButton>
            )
            } */}
        </div>
    );
}

export default FormView;
